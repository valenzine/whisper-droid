# Copy this file to keystore.properties and fill in values for local release builds.
# Keep keystore.properties and the keystore outside version control.
storeFile=/absolute/path/to/whisper-droid-upload.jks
storePassword=replace-with-keystore-password
keyAlias=whisper-droid-upload
keyPassword=replace-with-key-password
